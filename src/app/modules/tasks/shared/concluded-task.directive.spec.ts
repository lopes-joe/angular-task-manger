import { ConcludedTaskDirective } from './concluded-task.directive';

describe('ConcludedTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new ConcludedTaskDirective();
    expect(directive).toBeTruthy();
  });
});
